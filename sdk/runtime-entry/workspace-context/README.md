# Workspace Context

Role: route workspace root, checkout, override, and mirror-boundary posture.

Input: workspace discovery, `.aoa/workspace.toml`, source/runtime mirror
wording, bootstrap behavior, and workspace control-plane capsules.

Output: workspace implementation route, runtime-seam mechanic route, generated
capsule rebuild, or host/runtime owner handoff.

Owner: `sdk/runtime-entry/AGENTS.md` and
`sdk/source_home.manifest.json#workspace_context`.

Next route: `src/aoa_sdk/workspace/`, `.aoa/workspace.toml`,
`docs/workspace-layout.md`, and `mechanics/runtime-seam/`.

Stop line: do not make path guessing stronger than explicit workspace config.
