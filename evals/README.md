# Local Eval Port

This skeleton `evals/` port will preserve SDK-born eval pressure around typed
handoff helpers, compatibility checks, workspace discovery, and control-plane
boundaries.

`aoa-evals` owns central verdict, scoring, regression, and proof doctrine
authority. SDK helpers here may preserve local cases and reports, but they do
not become activation or proof authority.
