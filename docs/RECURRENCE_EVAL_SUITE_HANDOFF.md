# Recurrence Eval Suite Handoff

The first recurrence eval bundle lives in `aoa-evals`:

```text
bundles/aoa-recurrence-control-plane-integrity/
```

`aoa-sdk` should treat this as a proof consumer, not as part of the recurrence planner. Future SDK integration may add a `aoa recur eval-dossier` helper, but this seed does not add it.
