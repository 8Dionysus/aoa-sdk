# Agon Duel Kernel SDK Bridge

Wave XII gives `aoa-sdk` requested-only typed helper candidates for the duel kernel.

These helpers may load registries, draft local candidates, validate event shape, inspect hash-chain continuity, and export reviewable receipts. They do not activate a runtime service or issue outcomes.
