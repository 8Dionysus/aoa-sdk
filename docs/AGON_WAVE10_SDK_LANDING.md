# Agon Wave X SDK Landing

`aoa-sdk` receives requested-only typed helper candidates for CCS law inspection.

The SDK may help future tooling read and preview law surfaces. It must not become the arena runtime.
