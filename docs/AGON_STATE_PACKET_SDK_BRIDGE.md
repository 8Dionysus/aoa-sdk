# Agon State Packet SDK Bridge

Wave IX gives `aoa-sdk` requested-only helper candidates for future state packet tooling.

The SDK may later help load packet registries, validate packet shape, draft sealed commits, draft reveal views, inspect stop-lines and prepare review dossiers.

This seed does not wire live CLI commands, open arena sessions, submit packets to runtime, issue verdicts or mutate any owner repo.
