# Review Decision CLI Route

The recurrence review CLI and the part-local closure script own decision
template and queue-close entrypoints. Their executable operator and test route
is `../VALIDATION.md`.

The close command writes a close report, decision ledger, and suppression memory. Unresolved queue items remain explicit.
