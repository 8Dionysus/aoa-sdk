# Titan Praxis Replay

Replay uses visible Codex session exports to create candidate Titan packets and learning deltas.

It may consume visible dialogue, command rows, tool calls, patch calls and visible compaction markers.

It must not consume hidden reasoning, encrypted payloads or compacted replacement history.
