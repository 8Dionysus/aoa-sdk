# Titan Operator Console

The console keeps a visible roster, event ledger, gate approvals, digest, and optional app-server launch plan. It does not rewrite Codex agents and does not own role truth.

Commands: `roster`, `new`, `event`, `gate`, `digest`, `close`, `validate`, `appserver-plan`.
