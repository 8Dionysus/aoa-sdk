# Operator Console Helper Contracts Validation

Run:

```bash
python -m pytest -q mechanics/titan/parts/operator-console-helper-contracts/tests/test_titan_console.py
```

The repository-wide topology gate is owned by [root `VALIDATION.md`](../../../../VALIDATION.md#focused-repository-checks).
