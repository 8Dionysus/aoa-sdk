Recurrence wiring snippets live here as reviewable example material.

These files are not an installer and are not applied automatically.
Use them as selective copy candidates after inspecting the output of
`aoa recur wiring-plan` and the current owner repo's own boundaries.
